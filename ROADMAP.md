# 実装予定

## タスク管理の強化

| 優先度 | 機能 | 概要 | 状態 |
|--------|------|------|------|
| 高 | 担当者アサイン | `/task assign id @user` — 複数人に割り当て可能 | 未着手 |
| 高 | タスク検索 | `/task search キーワード` — タイトル・説明を全文検索 | 未着手 |
| 中 | タグ | `/task add` に `tag` パラメータ追加、`/task list` でタグ絞り込み | 未着手 |
| 中 | ソート切り替え | `/task list` に `sort` オプション（期限順・優先度順・作成日順） | 未着手 |
| 低 | サブタスク | タスクに子タスクをぶら下げ、親の完了率を自動計算 | 未着手 |

## 通知・リマインダーの強化

| 優先度 | 機能 | 概要 | 状態 |
|--------|------|------|------|
| 高 | 期限切れ通知 | 期限を過ぎた未完了タスクをチャンネルに通知 | 未着手 |
| 高 | DM通知オプション | リマインダーをチャンネルではなく担当者のDMに送る設定 | 未着手 |
| 中 | 毎日サマリー | 毎朝指定チャンネルに当日期限・未完了タスク一覧を投稿 | 未着手 |
| 低 | 完了時通知 | ステータスを「完了」にしたとき担当者全員にDMを送る | 未着手 |

## UI/表示の改善

| 優先度 | 機能 | 概要 | 状態 |
|--------|------|------|------|
| 高 | ページネーション | タスクが多いとき「次へ」ボタンで複数ページに分割表示 | 未着手 |
| 中 | インタラクティブUI | `/task view` にステータス変更・削除ボタンを埋め込み | 未着手 |
| 低 | 進捗バー | リスト表示でサブタスク完了率を `██░░░` 形式で表示 | 未着手 |

## インフラ・運用

| 優先度 | 機能 | 概要 | 状態 |
|--------|------|------|------|
| 高 | Docker対応 | `docker-compose.yml` + `Dockerfile` で1コマンド起動 | 未着手 |
| 中 | CSVエクスポート | `/task export` でサーバーのタスク一覧をCSVファイルとして送信 | 未着手 |
| 低 | 構造化ログ | `tracing` クレートで JSON ログ出力、ファイルローテーション | 未着手 |
